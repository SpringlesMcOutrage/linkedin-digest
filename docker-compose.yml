version: "3.9"

services:
  api:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      # Persist LinkedIn cookies between restarts
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - LINKEDIN_COOKIES_FILE=/app/data/linkedin_cookies.json
